/**
 * Écoute longue : conversations de 1 à 2 minutes par niveau (A1, A2, B1), avec des questions en français (A1),
 * mixtes (A2 : question en français, propositions en thaï) ou en thaï (B1). Rédigées puis relues deux fois
 * (langue thaïe et phonétique, puis pédagogie des questions).
 */
import type { Dialog } from '../types';

export const LONG_DIALOGS: Dialog[] = [];
